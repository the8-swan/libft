#include "libft.h"


char *ft_strnstr(const char *haystack, const char *needle, size_t len){
    char *ptr =NULL;
    size_t i=0;
    size_t j=0;
    if(needle[i] == '\0')
        return haystack;
    while(haystack[j] && j<len){
            if()
    }



    return ptr;    
}